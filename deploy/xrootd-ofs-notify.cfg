# Snippet for XRootD (e.g. /etc/xrootd/xrootd-access-tracer.cfg)
# Include from the main xrootd config.
# XRootD starts file-access-tracer once and pipes openr events to stdin.

ofs.notify openr | /usr/bin/file-access-tracer
